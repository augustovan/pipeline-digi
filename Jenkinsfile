pipeline {   agent any {    stages {      stage('Build') {        node ('master'){          stage ('Build on master'){              echo 'Running on master'              sh 'hostname; touch file.master'            }           }          }            }      }}