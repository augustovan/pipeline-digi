pipeline {   stages {      stage('Build') {        node ('master'){          stage ('staging'){              echo 'Running on master'              sh 'hostname; touch file.master'            }           }          }            }      }